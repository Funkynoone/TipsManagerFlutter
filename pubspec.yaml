name: tips_manager
description: A restaurant tips management application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Existing dependencies
  cupertino_icons: ^1.0.2
  table_calendar: ^3.0.9
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  provider: ^6.1.1

  # NEW dependencies for export functionality
  share_plus: ^7.2.1
  path_provider: ^2.1.1
  pdf: ^3.10.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true